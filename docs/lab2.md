# Лабораторная работа №2: Асимметричное шифрование (RSA) и Криптоанализ

## 📋 Описание задания
Целью работы является реализация криптосистемы с открытым ключом **RSA**, обеспечивающей шифрование и цифровую подпись данных. Особое внимание уделено безопасности генерации ключей и демонстрации уязвимостей алгоритма на примере **Атаки Винера** (Wiener's Attack) на малую секретную экспоненту.

### Реализованные компоненты:
1.  **Ядро RSA:** Реализация математики больших чисел (BigInt) для операций $M^e \pmod n$.
2.  **Генератор ключей:** Создание безопасных простых чисел $p, q$, вычисление $(e, d, n)$ с проверкой на стойкость.
3.  **Схема шифрования:** PKCS#1 v1.5 Padding для безопасного шифрования произвольных данных.
4.  **Атака Винера:** Алгоритм восстановления секретного ключа $d$ по открытому ключу $(e, n)$ при использовании небезопасных параметров.
5.  **Многопоточность:** Параллельное шифрование блоков файла для ускорения работы тяжелой арифметики RSA.

---

## 🏗 Архитектура и Технологии

### Библиотека больших чисел (BigInt)
RSA оперирует числами размером 1024, 2048 и 4096 бит. Стандартные типы C++ (`uint64_t`) для этого не подходят.
*   Использована библиотека **Boost.Multiprecision** (`cpp_int`) для эффективной работы с числами произвольной точности.
*   Она обеспечивает высокую производительность арифметических операций (умножение Карацубы, быстрое возведение в степень), что критично для RSA.

### Структура классов
*   `MathUtils`: Обертка над математическими примитивами. Реализует:
    *   Быстрое возведение в степень по модулю (`modPow`).
    *   Расширенный алгоритм Евклида для поиска обратного элемента (`modInverse`).
    *   Тест Миллера-Рабина для проверки чисел на простоту (`isPrime`).
*   `RSAKeyGenerator`: Отвечает за генерацию пар ключей. Включает в себя проверку на уязвимость к атаке Винера перед выдачей ключа.
*   `RSA_PKCS1`: Реализует стандарт выравнивания данных RFC 2313, превращая массив байт в число `BigInt`.
*   `WienerAttack`: Класс криптоанализа, реализующий алгоритм цепных дробей.

---

## 🧮 Математическое описание

### 1. Алгоритм RSA (Rivest–Shamir–Adleman)
Основан на вычислительной сложности задачи факторизации больших целых чисел.

**Генерация ключей:**
1.  Выбираются два больших простых числа $p$ и $q$.
2.  Вычисляется модуль $n = p \cdot q$.
3.  Вычисляется функция Эйлера $\phi(n) = (p-1)(q-1)$.
4.  Выбирается открытая экспонента $e$ (обычно 65537), такая что $1 < e < \phi(n)$ и $gcd(e, \phi(n)) = 1$.
5.  Вычисляется секретная экспонента $d \equiv e^{-1} \pmod{\phi(n)}$.

**Шифрование:**
Сообщение $M$ (преобразованное в число $m < n$) шифруется:
$$c = m^e \pmod n$$

**Дешифрование:**
$$m = c^d \pmod n$$
Корректность следует из теоремы Эйлера: $m^{ed} \equiv m^{k\phi(n)+1} \equiv m \pmod n$.

### 2. Схема дополнения PKCS#1 v1.5
RSA — детерминированный алгоритм. Шифрование одного и того же числа одним ключом всегда дает одинаковый шифротекст, что небезопасно.
Для защиты используется Padding (добавление случайных данных):
**Структура блока:** `0x00 || 0x02 || PS || 0x00 || Data`
*   `0x02`: Маркер типа операции (Encryption).
*   `PS`: Строка случайных ненулевых байт (минимум 8). Это делает шифрование вероятностным.
*   `Data`: Само сообщение.

### 3. Атака Винера (Wiener's Attack)
Майкл Винер в 1990 году показал, что если секретная экспонента $d$ слишком мала (для ускорения дешифрования на слабых устройствах), RSA полностью взламывается.

**Условие уязвимости:**
$$d < \frac{1}{3} n^{1/4}$$

**Суть атаки:**
Из уравнения $ed \equiv 1 \pmod{\phi(n)}$ следует существование $k$, такого что $ed - k\phi(n) = 1$.
Разделив на $d\phi(n)$, получаем:
$$\left| \frac{e}{n} - \frac{k}{d} \right| < \frac{1}{2d^2}$$
Это означает, что дробь $\frac{k}{d}$ является одной из **подходящих дробей** (конвергентов) разложения числа $\frac{e}{n}$ в непрерывную (цепную) дробь.

**Алгоритм реализации:**
1.  Разложить $\frac{e}{n}$ в цепную дробь $[q_0; q_1, q_2, \dots]$.
2.  Последовательно вычислять подходящие дроби $\frac{k_i}{d_i}$.
3.  Для каждого $d_i$ (кандидат на секретный ключ) проверять гипотезу:
    *   Вычислить $\phi' = (ed_i - 1) / k_i$.
    *   Решить квадратное уравнение $x^2 - (n - \phi' + 1)x + n = 0$.
    *   Если корни уравнения ($p'$ и $q'$) целые и $p' \cdot q' = n$, то ключ найден.

---

## 🛠 Сборка и Запуск

### Требования
*   Те же, что и для Лаб 1.
*   Библиотека **Boost**: `sudo apt install libboost-all-dev`

### Инструкция

1.  **Сборка:**
    ```bash
    ./scripts/build.sh
    ```
2.  **Запуск тестов (включая проверку математики и атаки):**
    ```bash
    ./scripts/run_tests.sh
    ```

3.  **Демонстрация RSA (Генерация -> Шифрование -> Дешифрование):**
    ```bash
    ./scripts/run_lab2.sh
    ```
    *Шифрует текстовый файл с помощью 1024-битного ключа.*

4.  **Демонстрация Атаки Винера:**
    ```bash
    ./scripts/run_lab2_attack.sh
    ```
    *Скрипт генерирует специально ослабленный ключ и восстанавливает его, зная только открытые данные.*

### CLI Интерфейс
```bash
./bin/lab2 <KEY_SIZE> <IN_FILE> <OUT_FILE> [demo|gen]
```
*   `demo`: Генерирует ключи в памяти, шифрует входной файл, затем расшифровывает его.
*   `gen`: Только генерирует ключи (заготовка для реального использования).

---

## 📦 Структура модулей

```text
include/crypto/
├── math/               # MathUtils (Primes, GCD, ModPow)
├── asymmetric/         # RSA, RSAKeyGenerator, WeakKeyGenerator (для атаки)
├── padding/            # RSA_PKCS1 (реализация RFC 2313)
├── attacks/            # WienerAttack (реализация на цепных дробях)
└── utils/              # RSAFileProcessor (многопоточное чтение/запись блоков)
```